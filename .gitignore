tau/data/matrices_7_7.h5
__pycache__/
CLAUDE.md
dev/
# pixi environments
.pixi/*
!.pixi/config.toml

# Cluster / run clutter
slurm-*.out
snakemake_logs/
logs/
core.[0-9]*
*.coredump

# Papers / screenshots / scratch caches in repo root
*.pdf
fig1_screenshot.png
workflow.png
gain_timing_oncogene_cache.tsv

# Demo / scratch outputs
tau_demo_out/

# FigHighCN render caches (regenerate from scripts; FIGHIGHCN_REBUILD=1 to force)
dev/pcawg/FigHighCN/.treecache/
dev/pcawg/FigHighCN/.rescache/
dev/pcawg/misc_data_and_plots/.bcache/
*.bak
*.bak_*
tau_timing.egg-info/
forJoseph.zip
notes_site/
# tau/data/solutions/ IS tracked: the per-state Sage solutions are what Tau times with,
# and the lazy per-state loader needs them (a monolithic .sobj cannot be loaded lazily).
tau/data/solutions_analytical/

# Legacy runs and example data from the pre-package era. Not needed to build or run Tau.
old_stuff/

# Local working files, not part of the package
# Manuscript working directory (1.3 GB of figures, caches and data tables)
paper/
# Local Claude Code settings and skills
.claude/
MEETING_PREP.md
tau_primer.md
resume.sh
submit_all_persig.sh
